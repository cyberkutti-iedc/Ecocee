/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://ecocee.in', // Replace with your website URL
  generateRobotsTxt: true, // Generate a robots.txt file
  generateIndexSitemap: true, // Optional: include an index sitemap
};

module.exports = config;
